<!-- Breadcrumbs-->
<ol class="breadcrumb">
  <li class="breadcrumb-item">
    <a href="#">Pedidos</a>
  </li>
  <li class="breadcrumb-item active">Meus Pedido</li>
</ol>
<div class="card mb-3">
  <div class="card-body">
   <table class="table table-bordered table-striped table-hover" id="dataTable" width="100%" cellspacing="0">
    <thead class="text-center">
    <tr>
      <th>Cliente</th>
      <th>Status</th>
      <th>Total</th>
      <th>#</th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.client.name %></td>
        <td><%= order.status %></td>
        <td><%= order.sub_total %></td>
        <td class='text-center  '> <%= link_to '', order , class:'btn  btn-warning fa fa-eye' %>    </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
<br>

<%= link_to 'Novo Pedido', venda_index_path, class: 'btn btn-primary' %>
