
<!-- Breadcrumbs-->
<ol class="breadcrumb">
  <li class="breadcrumb-item">
    <a href="#">Venda</a>
  </li>
  <li class="breadcrumb-item active">Nova venda</li>
</ol>
    <div class="col-xs-6 text-right">
        <h1 id="cart-text"><%= render 'layouts/cart_text' %></h1>
    </div>
<div class="card mb-3">
  <div class="card-body">
  <div class="table-responsive">
   <table class="table table-bordered table-striped table-hover" id="dataTable" width="100%" cellspacing="0">
    <thead class="text-center">
    <tr>
      <th> ID </th>
      <th>Nome</th>
      <th>Categoria</th>
      <th>Preço</th>
      <th>Quantidade</th>
      <th>Adicionar no carrinho</th>
    </tr>
  </thead>

  <tbody class="text-center">
    <% @products.each do |product| %>
      <%= render "product_row", product: product, order_item: @order_item, client: @client %>
    <% end %>
  </tbody>
</table>
</div>
</div>
</div>
